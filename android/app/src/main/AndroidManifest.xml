<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:label="flutter_daily_practice">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            // TODO(Note): This will help the android open to open a new route
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Custom scheme deep links: myapp://screenone?id=... -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="screenone"
                    android:scheme="myapp" />
                <data
                    android:host="screentwo"
                    android:scheme="myapp" />
            </intent-filter>

            <!--
                App Links (HTTPS deep links): https://yourdomain.com/screenone?id=...

                android:autoVerify="true" is the key difference from a plain deep link:
                  • Without it → Android shows a disambiguation dialog ("Open with...")
                  • With it    → Android verifies domain ownership via assetlinks.json
                                 and opens your app directly (no dialog)

                For verification to work you must host:
                  https://yourdomain.com/.well-known/assetlinks.json

                To test locally without a server, use adb (bypasses verification):
                  adb shell am start -W -a android.intent.action.VIEW \
                    -d "https://yourdomain.com/screenone?id=99" \
                    com.example.flutter_daily_practice
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="https"
                    android:host="yourdomain.com" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>

    </queries>
</manifest>
